<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Article</title>
  </head>
  <body>
    <h1><%= article.title %></h1>
    <h2><%= article.author %> <%= article.major %> <%= article.year %></h2>
    <% var bodyCount = 0; %>
    <% var pullCount = 0; %>
    <% for(let element of article.elementOrder) { %>
        <% switch (element) {
            case "body": %>
                <p><%= article.body[bodyCount] %></p>
                <% bodyCount++; %>
                <% break;
            case "pull": %>
                <p><%= article.pullquotes[pullCount] %></p>
                <% pullCount++; %>
                <% break;
            default:
                break; %>
        <% } %>
    <% } %>
  </body>
</html>
